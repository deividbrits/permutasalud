# MongoDB Configuration (Deprecated)
# MONGODB_URI=mongodb://localhost:27017/permutasalud

# Firebase Configuration
# Make sure you have downloaded the serviceAccountKey.json to the root folder.
# The server will automatically use that file to authenticate with Firebase Admin SDK.

VITE_FIREBASE_STORAGE_BUCKET="your-storage-bucket"
VITE_FIREBASE_MESSAGING_SENDER_ID="your-messaging-sender-id"
VITE_FIREBASE_APP_ID="your-app-id"
JWT_SECRET="generate_a_random_secret_key_here"
